{
  "name": "surfmind-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:awesome": "npm run generate:catalog",
    "generate:catalog": "tsx scripts/generate-catalog.ts",
    "lint": "tsx scripts/validate.ts",
    "test": "tsx --test scripts/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "yaml": "^2.7.1",
    "zod": "^3.24.4"
  }
}
